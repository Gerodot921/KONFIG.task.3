## 📘 Описание проекта

Этот проект — минималистичный симулятор процессора. Он поддерживает:
- работу с регистрами (256 целочисленных регистров),  
- память (массив из 5000 слов),  
- две машинные команды:  
  - `STORE`: сохраняет значение регистра в память по адресу `reg[B] + D`,  
  - `SHIFT LEFT`: считывает значение из памяти по адресу `reg[B] + E`, сдвигает влево на `reg[D]` бит и записывает результат по адресу `reg[C]`.  
- ассемблер для кодирования команд в байтовый формат,  
- простой REPL-интерфейс для интерактивной работы, установки регистров/памяти, выполнения команд, просмотра состояния.

## ⚙️ Функционал  

- `store A B C D` — команда STORE, 5‑байтный формат  
- `shift A B C D E` — команда SHIFT LEFT, 6‑байтный формат  
- `reg i value` — установить регистр `i` в `value`  
- `mem addr value` — записать `value` по адресу памяти `addr`  
- `print reg i` / `print mem addr` — вывести значение регистра или памяти  
- `dump reg` — вывести все регистры, содержащие ненулевые значения  
- `dump mem start end` — вывести значения памяти в диапазоне `[start..end]`  
- `help` — показать справку  
- `exit` — выйти из REPL
